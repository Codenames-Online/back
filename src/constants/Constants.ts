export var BLUE = 0;
export var RED = 1;
export var NEUTRAL = 2;
export var ASSASSIN = 3;
export var UNCOLORED = 4;

export enum Turn {
  spy = 0,
  op = 1,
}

export enum Team {
  blue = 0,
  red = 1,
}
