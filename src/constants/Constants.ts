export enum Color {
  blue = 0,
  red = 1,
  neutral = 2,
  assassin = 3,
  uncolored = 4
}

export enum Team {
  blue = 0,
  red = 1,
}

export enum Turn {
  spy = 0,
  op = 1,
}

export enum PlayerLocation {
  loner = 0,
  lobby = 1,
  game = 2
}